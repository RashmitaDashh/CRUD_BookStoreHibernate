package com.hibernateCrud.CRUD_hibernate;

/**
 * Hello world!
 *
 */
public class App {

	/*
	 * public static void main(String[] args) { Configuration cfg = new
	 * Configuration();
	 * cfg.configure("/com/hibernateCrud/CRUD_hibernate/config/hibernate.cfg.xml");
	 * 
	 * SessionFactory sessionFactory = cfg.buildSessionFactory();
	 * 
	 * Session session = sessionFactory.openSession();
	 * 
	 * 
	 * // Create/Insert user //User user1 = new User("Celia", "Cel05@xcd.com",
	 * "80:thd", "Male", "Sydney");
	 * 
	 * User user2 = new User(); user2.setName("Daryl"); user2.setPassword("lb5@#4");
	 * user2.setEmail("Dar8@fb.com"); user2.setGender("male");
	 * user2.setCity("Mexico");
	 * 
	 * try { Transaction transaction = session.beginTransaction();
	 * //session.save(user1); session.save(user2); transaction.commit(); } catch
	 * (Exception e) { transaction.rollback(); e.printStackTrace(); } finally {
	 * session.close(); }
	 * 
	 * 
	 * 
	 * //SELECT/READ user try { User user = session.get(User.class, 1);
	 * System.out.println(user); } catch(Exception e) { e.printStackTrace(); }
	 * 
	 * 
	 * 
	 * //UPDATE/EDIT user Transaction transaction = session.beginTransaction(); try
	 * { User user3 = session.get(User.class, 4); user3.setCity("New Hamshire");
	 * user3.setName("Daniel"); user3.setEmail("Dan23@ort.com");
	 * session.update(user3); transaction.commit(); } catch(Exception e) {
	 * transaction.rollback(); e.printStackTrace(); } finally { session.close(); }
	 * 
	 * 
	 * 
	 * //DELETE user Transaction transaction = session.beginTransaction(); try {
	 * User user4 = session.get(User.class, 4);
	 * System.out.println("Information of deleted user:"+"\n");
	 * System.out.println(user4); session.delete(user4); transaction.commit();
	 * System.out.println("***********USER DELETED**************"); }
	 * catch(Exception e) { transaction.rollback();
	 * System.out.println("No such user exists......."); e.printStackTrace(); }
	 * finally { session.close(); }
	 * 
	 * 
	 * }
	 */

}
